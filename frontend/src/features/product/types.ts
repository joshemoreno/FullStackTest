export type Product = {
  productId: string;
  name: string;
  description: string;
  imageUrl: string;
  price_in_cents: number;
  stock: number;
  active: boolean;
  createdAt: string;
  updatedAt: string;
};
